<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.healthcare">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".section7"></activity>
        <activity android:name=".section6" />
        <activity android:name=".section5" />
        <activity android:name=".section4" />
        <activity android:name=".section3" />
        <activity android:name=".section2" />
        <activity android:name=".StratActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CheckActivity" />
        <activity android:name=".ListViews" />
        <activity android:name=".Dr_profile" />
    </application>

</manifest>